<% layout('layouts/boilerplate') %>

<!-- Map Section -->
<div class="container-fluid px-0">
  <div id="map-overlay">
    <div id="map" style="width: 100%; height: 400px"></div>
  </div>
</div>

<!-- Main Content -->
<div class="campgrounds-header">
  <div class="container">
    <!-- Flash Messages -->
    <% if (success && success.length) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    
    <% if (error && error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <!-- Header -->
    <div class="row align-items-center mb-5">
      <div class="col-md-8">
        <h1 class="mb-3">All Campgrounds</h1>
        <p class="lead">Discover amazing camping spots shared by our community</p>
      </div>
      <div class="col-md-4 text-md-end">
        <a href="/campgrounds/new" class="btn btn-primary">
          Add New Campground
        </a>
      </div>
    </div>
  </div>
</div>

<!-- Campgrounds Grid -->
<div class="campgrounds-grid">
  <div class="container">
    <% if (campgrounds.length === 0) { %>
      <div class="text-center py-5">
        <h3 class="text-muted mb-3">No campgrounds yet</h3>
        <p class="text-muted mb-4">Be the first to share an amazing camping spot!</p>
        <a href="/campgrounds/new" class="btn btn-primary">Add First Campground</a>
      </div>
    <% } else { %>
      <div class="row g-4">
        <% for (let campground of campgrounds) { %>
        <div class="col-lg-4 col-md-6">
          <div class="card campground-card h-100">
            <!-- Image Container -->
            <div class="position-relative" style="height: 240px; overflow: hidden;">
              <img crossorigin="anonymous" 
                   src="<%= campground.images.length != 0 ? campground.images[0].url : 'https://images.unsplash.com/photo-1504280390367-361c6d9f38f4?w=600&h=400&fit=crop' %>"
                   class="card-img-top w-100 h-100"
                   style="object-fit: cover;"
                   alt="<%= campground.title %>">
              
              <!-- Price Badge -->
              <div class="price-badge">
                $<%= campground.price %>/night
              </div>
            </div>

            <!-- Card Body -->
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= campground.title %></h5>
              
              <p class="location-text mb-3">
                📍 <%= campground.location %>
              </p>
              
              <p class="card-text flex-grow-1 mb-4">
                <%= campground.description ? campground.description.substring(0, 120) + "..." : "Discover this amazing campground location." %>
              </p>
              
              <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary w-100">
                View Details
              </a>
            </div>
          </div>
        </div>
        <% } %>
      </div>
    <% } %>
  </div>
</div>

<%- "<script>" %>
const maptilerApiKey = "<%- process.env.MAPTILER_API_KEY %>";
const campgrounds = {
  type: "FeatureCollection",
  features: <%- JSON.stringify(
    campgrounds.map(campground => ({
      type: "Feature",
      geometry: campground.geometry,
      properties: { popUpMarkup: campground?.properties?.popUpMarkup }
    }))
  ) %>
};
<%- "</script>" %>

<script src="/javascripts/clusterMap.js"></script>
